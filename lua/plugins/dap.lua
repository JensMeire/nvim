-- Central dap plugin spec, with modular language configs
return {
  "mfussenegger/nvim-dap",
  config = function()
    -- Each of these sets up dap for a language
  end,
  -- You can add keys, dependencies, event, or opts fields here
}
